(define (problem test-4-1)
(:domain blocks)
(:objects D C B A - object)
(:init
	(handempty) (ontable D) (on C D) (on B C) (on A B) (clear A)       
       )
(:goal (AND
(not (handempty))
	   (not (holding A))
	   (not (holding B))
	   (not (holding C))
	   (holding D)

       	   (ontable A)
           (not (ontable B))
           (not (ontable C))
           (not (ontable D))
	   (not (on A A))
	   (not (on A B))
	   (not (on A C))
	   (not (on A D))	   	   

           (on B A)
           (not (on B B))
           (not (on B C))
           (not (on B D))

	   (not (on C A))
	   (on C B)	   
	   (not (on C C))	   	   
	   (not (on C D))

	   (not (on D A))
	   (not (on D B))
	   (not (on D C))
	   (not (on D D))	  

	   (not (clear A))
	   (not (clear B))
	   (clear C)
	   (not (clear D))
	   )))